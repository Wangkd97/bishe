<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ysu.tour.dao.UserActiveMapper" >

    <resultMap id="BaseResultMap" type="com.ysu.tour.pojo.UserActive" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="a_id" property="aId" jdbcType="INTEGER" />
        <result column="u_id" property="uId" jdbcType="INTEGER" />
        <result column="u_click_num" property="uClickNum" jdbcType="INTEGER" />
        <result column="u_comment_num" property="uCommentNum" jdbcType="INTEGER" />
        <result column="u_look_num" property="uLookNum" jdbcType="INTEGER" />
        <result column="u_active_num" property="uActiveNum" jdbcType="INTEGER" />
    </resultMap>

    <select id="selectByuId" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select * from user_active_table where u_id=#{uId}

    </select>

    <update id="updateClick" parameterType="java.lang.Integer">
        update user_active_table set
        u_click_num = u_click_num+1 where u_id=#{uId}
    </update>

    <update id="updateComment" parameterType="java.lang.Integer">
        update user_active_table set
        u_comment_num = u_comment_num+1 where u_id=#{uId}
    </update>

    <update id="updateLook" parameterType="java.lang.Integer">
        update user_active_table set
        u_look_num = u_look_num+1 where u_id=#{uId}
    </update>

    <update id="updateActive" parameterType="map">
        update user_active_table set
        u_active_num = #{userActiveNum} where u_id=#{uId}
    </update>
    <insert id="insert" parameterType="java.lang.Integer">
        insert into user_active_table (u_id,u_click_num,u_comment_num,u_look_num,u_active_num)
        values(#{uId},0,0,0,0)
    </insert>

    <select id="selectAll">
        select * from user_active_table
    </select>



</mapper>